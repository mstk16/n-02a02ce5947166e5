# このリポジトリでの作業ルール

## 配色・コントラストの確認(必須)

背景色と文字色が同化して読めなくなる不具合は、レビューを素通りしやすい割に
致命的なので、以下を必ず守る。

1. **新規/変更した要素はすべて、背景色と文字色の組み合わせを明示的に確認する。**
   「デフォルトのままだから大丈夫」で済ませない。特にダークテーマ基調の
   このアプリでは、外部由来のUIが白背景で出てくることが多いので要注意。
2. **自分でCSSを書かない外部埋め込み(iframe等)は、色を制御できない前提で扱う。**
   - 埋め込み先が公式にテーマ指定パラメータを提供している場合は必ず使う
     (例: YouTubeライブチャット埋め込みは `&dark_theme=1` を付ける)。
   - パラメータがない場合は、埋め込み枠自体の背景色をこちらのテーマに
     合わせた色で明示的に敷いておく(白背景が透けて見えるのを避ける)。
   - 「たぶん大丈夫」で出荷しない。公式ドキュメント/パラメータを確認する。
3. **このサンドボックス環境はネットワーク制限があり、youtube.com等の外部
   サイトを実際にレンダリングして見た目を確認できない。** 外部埋め込みが
   絡む変更を報告するときは、「コード上は対策したが実機での見た目確認は
   していない」ことを明言し、ユーザーに実機確認を依頼する。「動作確認済み」
   と紛らわしい言い方をしない。
4. コミット前のセルフレビューで、変更したCSSの中に
   `color` と `background`(または類似の背景指定)が同系色・同一色に
   なっている箇所がないか、意識的に見直す。

このルールは配信メモアプリ(`memo/`)に限らず、このリポジトリでの
すべてのUI変更に適用する。
